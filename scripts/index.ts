import { Level } from "level";
import path from "path"

async function main() {
    const db_path = path.join(__dirname,"../chaindata")

    
}

(async () => {
    await main();
    process.exit(0);
  })();
  